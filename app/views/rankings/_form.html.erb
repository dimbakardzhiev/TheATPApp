<%= form_with(model: ranking, local: true) do |form| %>
  <% if ranking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ranking.errors.count, "error") %> prohibited this ranking from being saved:</h2>

      <ul>
      <% ranking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :month %>
    <%= form.text_field :month, id: :ranking_month %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :ranking_year %>
  </div>

  <div class="field">
    <%= form.label :ranking %>
    <%= form.number_field :ranking, id: :ranking_ranking %>
  </div>

  <div class="field">
    <%= form.label :player_id %>
    <%= form.number_field :player_id, id: :ranking_player_id %>
  </div>

  <div class="field">
    <%= form.label :points %>
    <%= form.number_field :points, id: :ranking_points %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

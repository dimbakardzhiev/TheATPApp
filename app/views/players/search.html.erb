<%if params[:last_name] == "" %>
	<%= flash.now[:alert] = "Player not found" %>
<% else %>

	<% @players.each do |player| %>
		<%= player.first_name %><br>
		<%= player.last_name %>
	<% end %>
<% end %>